const ParamsDTO = require("./params.dto");
const UserDTO = require('./user.dto');
const TechnologyDTO = require('./technology.dto');
const QualificationDTO = require('./qualification.dto');
const FriendshipDTO = require('./friendship.dto')

module.exports = {
    ParamsDTO,
    UserDTO,
    TechnologyDTO,
    QualificationDTO,
    FriendshipDTO
}